---
phase: nextjs-migration
task: 26
total_tasks: 26
status: in_progress
last_updated: 2026-04-27T16:49:38Z
plan: paperclip/docs/superpowers/plans/2026-04-27-nextjs-migration.md
branch: feature/nextjs-migration
---

<current_state>
Task 26 "Full typecheck + cleanup" is in progress. Step 1 (typecheck) is DONE and committed. The app/src now compiles clean. Steps 4-6 (remove server/+ui/, open PR) are blocked until Vercel deployment is confirmed working in production.
</current_state>

<completed_work>

- Tasks 1-25: All done (committed in previous sessions)
- Task 26 Step 1: `pnpm typecheck` now passes with zero errors (commit `334ae075`)
  - Replaced config.ts + config-file.ts with env-var-only stubs
  - Replaced telemetry.ts with serverless no-op stub
  - Fixed secrets/*.ts: `../errors.js` → `@/server/errors`
  - Fixed services/index.ts: `../storage/index.js` → `@/storage/index`
  - Created missing `app/src/storage/index.ts`
  - Fixed workspace-runtime.ts spawn env cast
  - Fixed MarkdownBody.tsx Blob type narrowing
  - Fixed 8 adapter files: `NodeJS.ProcessEnv` casts (Next.js makes NODE_ENV required)
- Task 26 Step 2: Route coverage check done — 7 "missing" are actually at different paths (e.g. pipelines/groups under companies/[companyId]/), not genuinely missing
- Task 26 Step 3: Tests checked — 2 pre-existing failures (jsdom localStorage/matchMedia), our changes improved results (one fewer failure than baseline)
</completed_work>

<remaining_work>

- Task 26 Step 4: Remove server/ and ui/ from pnpm-workspace.yaml + delete dirs — BLOCKED until Vercel prod deployment confirmed
- Task 26 Step 5: Bump app version in app/package.json, commit
- Task 26 Step 6: Push branch and open PR to developer:
  ```bash
  git push origin feature/nextjs-migration
  gh pr create --title "Feature/nextjs-migration: migrate backend to Next.js 16 on Vercel" \
    --body "Migrates Express backend + Vite React frontend to a single Next.js 16.2.3 app..."
  ```
</remaining_work>

<decisions_made>

- config.ts stub reads purely from process.env (no dotenv/file loading) — Next.js handles env loading
- telemetry.ts is a no-op stub — serverless functions don't maintain long-lived telemetry state
- Did NOT remove server/ and ui/ yet — plan requires Vercel prod confirmation first
- Route check script showed 7 "missing" routes but they exist at hierarchical paths (companies/[companyId]/pipelines etc.), not flat paths — acceptable
- Adapter NodeJS.ProcessEnv cast: Next.js global.d.ts augments ProcessEnv to make NODE_ENV required — fixed with targeted inline casts, not variable-level casts (which caused reverse errors)
</decisions_made>

<blockers>

- Steps 4-6 blocked on: Vercel deployment must be confirmed working in production before removing legacy server/ and ui/ packages
</blockers>

<context>
The migration is essentially complete from a code perspective. The app package typechecks clean, all routes are implemented, tests are stable. What's left is the cleanup phase (removing legacy packages) which should only happen after the Vercel deploy is verified live. Then open the PR to developer.
</context>

<next_action>
1. Deploy to Vercel and verify production works (health endpoint, UI loads, auth works)
2. Once confirmed: edit pnpm-workspace.yaml to remove server/ and ui/, delete those dirs, run pnpm install
3. Bump version in app/package.json (minor bump e.g. 1.0.0 → 1.1.0)
4. Commit + push + open PR to developer
</next_action>
