# Local dev setup — backend on port 3100, Vite UI on port 5173
# Usage:
#   ./scripts/dev-local.sh
#   Or manually: docker compose -f docker/docker-compose.dev.yml up -d && pnpm --filter @paperclipai/ui dev


services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: paperclip
      POSTGRES_PASSWORD: paperclip
      POSTGRES_DB: paperclip
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U paperclip -d paperclip"]
      interval: 2s
      timeout: 5s
      retries: 30
    ports:
      - "5432:5432"
    volumes:
      - pgdata-dev:/var/lib/postgresql/data

  server:
    image: pacosemino/paperclip-backend:dev
    ports:
      - "3100:3100"
    environment:
      DATABASE_URL: postgres://paperclip:paperclip@db:5432/paperclip
      PORT: "3100"
      HOST: "0.0.0.0"
      SERVE_UI: "false"
      PAPERCLIP_MIGRATION_AUTO_APPLY: "true"
      PAPERCLIP_DEPLOYMENT_MODE: "authenticated"
      PAPERCLIP_DEPLOYMENT_EXPOSURE: "public"
      PAPERCLIP_PUBLIC_URL: "http://localhost:3100"
      PAPERCLIP_AUTH_PUBLIC_BASE_URL: "http://localhost:3100"
      PAPERCLIP_AUTH_BASE_URL_MODE: "explicit"
      PAPERCLIP_EXTRA_ORIGINS: "tauri://localhost,http://localhost:5173"
      BETTER_AUTH_TRUSTED_ORIGINS: "tauri://localhost,http://localhost:5173"
      BETTER_AUTH_SECRET: "${BETTER_AUTH_SECRET:-dev-secret-change-in-prod}"
    volumes:
      - paperclip-data-dev:/paperclip
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata-dev:
  paperclip-data-dev:
