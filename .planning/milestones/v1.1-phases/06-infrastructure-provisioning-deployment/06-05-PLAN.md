---
phase: 06-infrastructure-provisioning-deployment
plan: 05
type: execute
wave: 2
depends_on: [06-04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/06-infrastructure-provisioning-deployment/06-03-SUMMARY.md
autonomous: false
gap_closure: true
requirements: [AUTH-05]

must_haves:
  truths:
    - "A user can sign up from the Vercel frontend (paperclip-beige-five.vercel.app) to the Easypanel backend"
    - "A user can sign in from the Vercel frontend and receive a session cookie"
    - "Session persists across page refresh (F5) without requiring re-login"
    - "No [object Object] rendering bug visible in the Vercel frontend UI"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "AUTH-05 marked as complete"
      contains: "[x] **AUTH-05**"
    - path: ".planning/phases/06-infrastructure-provisioning-deployment/06-03-SUMMARY.md"
      provides: "AUTH-05 added back to requirements-completed after verification"
      contains: "AUTH-05"
  key_links:
    - from: "Vercel frontend (paperclip-beige-five.vercel.app)"
      to: "Easypanel backend (paperclip-paperclip-api.qiwa34.easypanel.host)"
      via: "POST /api/auth/sign-up/email and POST /api/auth/sign-in/email"
      pattern: "fetch.*api/auth"
    - from: "BetterAuth session cookie"
      to: "GET /api/auth/get-session"
      via: "Cookie with SameSite=None; Secure persists across refresh"
      pattern: "get-session.*200"
---

<objective>
Verify end-to-end authentication from the Vercel frontend to the Easypanel backend. Bootstrap the first admin user, confirm sign-up, sign-in, and session persistence. Mark AUTH-05 as complete upon successful verification.

Purpose: AUTH-05 is the last Phase 6 requirement. The infrastructure is deployed but auth was never verified end-to-end. The [object Object] rendering bug may have been fixed by post-plan commits but needs human confirmation. This checkpoint closes the final gap.

Output: AUTH-05 marked complete in REQUIREMENTS.md; 06-03-SUMMARY.md updated with AUTH-05 in requirements-completed.
</objective>

<execution_context>
@/Users/pacosemino/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pacosemino/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-provisioning-deployment/06-03-SUMMARY.md
@.planning/phases/06-infrastructure-provisioning-deployment/06-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Bootstrap admin and verify auth flow on live deployment</name>
  <files>N/A - human verification checkpoint</files>
  <read_first>
    .planning/phases/06-infrastructure-provisioning-deployment/06-VERIFICATION.md
  </read_first>
  <action>
Present the following verification steps to the user and wait for their confirmation.

The full three-tier deployment is live:
- Backend: https://paperclip-paperclip-api.qiwa34.easypanel.host/
- Frontend: https://paperclip-beige-five.vercel.app
- Backend health: GET /api/health returns 200 with bootstrapStatus (currently "bootstrap_pending")
- CORS is configured to allow the Vercel domain
- BetterAuth cookies are set to SameSite=None; Secure

Post-plan bug fixes already applied:
- Commit 37e23e99: Fixed health endpoint to use API_BASE
- Commit b2c7167a: Added SPA rewrite to root vercel.json and /my-tasks redirect

Ask the user to perform these steps:

**Step 1: Check if [object Object] bug is resolved**
- Open https://paperclip-beige-five.vercel.app in your browser
- Verify no "[object Object]" text appears in red
- Expected: You should see either a Bootstrap Pending page or a login/sign-up page
- If [object Object] still appears: note this and we will diagnose

**Step 2: Check backend health**
- Open https://paperclip-paperclip-api.qiwa34.easypanel.host/api/health in browser or run:
  `curl -s https://paperclip-paperclip-api.qiwa34.easypanel.host/api/health | jq .`
- Expected: HTTP 200 with `{"status": "ok", "bootstrapStatus": "bootstrap_pending" or "ready"}`

**Step 3: Bootstrap first admin (if bootstrapStatus is "bootstrap_pending")**
- From the project root, run the CLI bootstrap command to create the first admin user:
  `pnpm --filter @paperclipai/server relaycontrol auth-bootstrap-ceo --email YOUR_EMAIL --password YOUR_PASSWORD --name "Your Name"`
- Or use the bootstrap UI if the BootstrapPendingPage provides a form
- After bootstrap, re-check /api/health -- bootstrapStatus should change to "ready"

**Step 4: Sign up / Sign in from Vercel frontend**
- Open https://paperclip-beige-five.vercel.app
- Sign in with the admin credentials you just created (or sign up a new user if bootstrap created the initial company)
- Expected: Successful login, redirected to dashboard or My Tasks page
- Check browser console (F12 > Console) for any CORS errors or 403 responses

**Step 5: Verify session persistence**
- After signing in, press F5 (page refresh)
- Expected: You remain logged in (not redirected to login page)
- Open DevTools > Application > Cookies for the Easypanel domain
- Expected: BetterAuth session cookie present with SameSite=None and Secure flag
- Open DevTools > Network tab, find the GET /api/auth/get-session request
- Expected: Returns HTTP 200 with session data (not 401 or null)

**User should report back with:**
- Did [object Object] appear? (yes/no)
- What was bootstrapStatus before and after bootstrap?
- Did sign-in succeed? (yes/no + any console errors)
- Did session persist after F5? (yes/no)
- Cookie attributes observed (SameSite, Secure, domain)

Resume signal: User types "auth verified" if all checks pass, or describes any issues encountered.
  </action>
  <verify>User confirms all 5 steps passed by typing "auth verified"</verify>
  <acceptance_criteria>
    - No [object Object] rendering bug visible
    - Backend health returns 200
    - Bootstrap completed (if needed) -- bootstrapStatus shows "ready"
    - Sign-in from Vercel frontend succeeds without CORS errors
    - Session persists after page refresh
    - BetterAuth cookie has SameSite=None and Secure attributes
  </acceptance_criteria>
  <done>Human has verified end-to-end auth flow works from Vercel frontend to Easypanel backend. Bootstrap completed, session persists.</done>
</task>

<task type="auto">
  <name>Task 2: Mark AUTH-05 complete and update SUMMARY</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/06-infrastructure-provisioning-deployment/06-03-SUMMARY.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/06-infrastructure-provisioning-deployment/06-03-SUMMARY.md
  </read_first>
  <action>
After human verification confirms auth works:

1. In .planning/REQUIREMENTS.md:
   - Change AUTH-05 from `- [ ] **AUTH-05**: User can sign up and log in from Vercel-hosted frontend to Railway-hosted backend`
   - To: `- [x] **AUTH-05**: User can sign up and log in from Vercel-hosted frontend to Easypanel-hosted backend`
   - In the Traceability table: change AUTH-05 status from "Pending" to "Complete"

2. In .planning/phases/06-infrastructure-provisioning-deployment/06-03-SUMMARY.md:
   - Add AUTH-05 back to the `requirements-completed` frontmatter list (it should now read `[DEPLOY-05, DEPLOY-07, AUTH-05]`)
   - In the Accomplishments section, update the auth line to confirm: "Cross-origin auth verified end-to-end: sign-up, sign-in, and session persistence confirmed from Vercel frontend to Easypanel backend (verified in gap closure plan 06-05)"
  </action>
  <verify>
    <automated>grep "\[x\] \*\*AUTH-05" .planning/REQUIREMENTS.md && grep "requirements-completed.*AUTH-05" .planning/phases/06-infrastructure-provisioning-deployment/06-03-SUMMARY.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "[x] **AUTH-05**" in REQUIREMENTS.md returns a match containing "Easypanel"
    - AUTH-05 row in Traceability table shows "Complete"
    - 06-03-SUMMARY.md requirements-completed frontmatter includes AUTH-05
    - All six Phase 6 requirements now show [x] in REQUIREMENTS.md
  </acceptance_criteria>
  <done>AUTH-05 marked complete. All six Phase 6 requirements are now [x] in REQUIREMENTS.md. Phase 6 is fully closed.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -c "\[x\]" .planning/REQUIREMENTS.md` returns count including all Phase 6 requirements
2. `grep "AUTH-05" .planning/REQUIREMENTS.md` shows [x] with Easypanel reference
3. All six Phase 6 requirements (DEPLOY-05, DEPLOY-07, DEPLOY-09, DEPLOY-10, DEPLOY-11, AUTH-05) show [x]
4. Human confirmed: sign-up, sign-in, session persistence working from Vercel to Easypanel
</verification>

<success_criteria>
- Human has verified end-to-end auth flow from Vercel frontend to Easypanel backend
- Bootstrap completed -- first admin user exists
- No [object Object] rendering bug in the UI
- AUTH-05 marked [x] complete in REQUIREMENTS.md with Easypanel reference
- 06-03-SUMMARY.md includes AUTH-05 in requirements-completed
- All six Phase 6 requirements are now complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-provisioning-deployment/06-05-SUMMARY.md`
</output>
