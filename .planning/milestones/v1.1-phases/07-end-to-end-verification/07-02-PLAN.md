---
phase: 07-end-to-end-verification
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - .planning/phases/07-end-to-end-verification/07-VERIFICATION.md
autonomous: false
requirements: [E2E-01, E2E-02, E2E-03, E2E-04, E2E-05, E2E-06]

must_haves:
  truths:
    - "Owner can generate and copy an invite link from the Company Settings page on the live Vercel frontend"
    - "A new user can follow the invite link, sign up with a real email, accept the invite, and land on the home page"
    - "The invited user sees their My Tasks dashboard (initially empty, then populated after owner assigns a task)"
    - "Owner can assign a task to the invited user using the assignee picker"
    - "The invited user can change task status, attach a file, and create a subtask — all persist after page reload"
    - "The invited user can reassign a task to an AI agent and the assignee updates to show the agent"
    - "Real-time WebSocket updates are visible: a status change by one user appears for the other user without page refresh"
  artifacts:
    - path: ".planning/phases/07-end-to-end-verification/07-VERIFICATION.md"
      provides: "Pass/fail report for all 6 E2E requirements with screenshot references"
      contains: "E2E-01"
    - path: ".planning/phases/07-end-to-end-verification/screenshots/"
      provides: "Screenshot evidence for each verification checkpoint"
  key_links:
    - from: "Vercel frontend (https://paperclip-beige-five.vercel.app)"
      to: "Easypanel backend (https://paperclip-paperclip-api.qiwa34.easypanel.host)"
      via: "VITE_API_URL cross-origin fetch with credentials:include"
      pattern: "api-base\\.ts.*VITE_API_URL"
    - from: "LiveUpdatesProvider WebSocket"
      to: "Easypanel backend WSS"
      via: "getWsHost() derives host from VITE_API_URL"
      pattern: "wss://.*events/ws"
---

<objective>
Execute the complete multi-user E2E verification journey on the live deployment (Vercel + Easypanel + Supabase) using Chrome DevTools MCP. Verify all 6 E2E requirements in sequence: owner invites user, user joins, owner assigns task, user works task, user hands off to AI agent, real-time updates observed. Produce a VERIFICATION.md report with pass/fail per requirement and screenshot evidence.

Purpose: This is the gate for Phase 7 completion. All 6 E2E requirements must pass before the phase closes. The user reviews the VERIFICATION.md and gives final sign-off.
Output: VERIFICATION.md with pass/fail status and screenshots in the screenshots/ directory.
</objective>

<execution_context>
@/Users/pacosemino/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pacosemino/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-end-to-end-verification/07-CONTEXT.md
@.planning/phases/07-end-to-end-verification/07-RESEARCH.md
@.planning/phases/07-end-to-end-verification/07-01-SUMMARY.md

<interfaces>
<!-- Live deployment URLs (from Phase 6 memory) -->
Frontend: https://paperclip-beige-five.vercel.app
Backend: https://paperclip-paperclip-api.qiwa34.easypanel.host
Health: https://paperclip-paperclip-api.qiwa34.easypanel.host/api/health

<!-- Key UI routes for the E2E journey -->
Company Settings (invite generation): /<company-prefix>/settings
Invite Landing: /invite/<token>
My Tasks: /<company-prefix>/my-tasks
Issue Detail: /<company-prefix>/issues/<issue-id>

<!-- Key API endpoints used during verification -->
POST /api/companies/:companyId/invites — creates invite (requires users:invite)
GET /api/companies/:companyId/members — lists members (fixed in 07-01)
PATCH /api/issues/:id — update issue (assignee, status)
POST /api/companies/:companyId/issues — create issue/subtask
POST /api/companies/:companyId/issues/:issueId/attachments — file upload
WebSocket: wss://paperclip-paperclip-api.qiwa34.easypanel.host/api/companies/:companyId/events/ws

<!-- Chrome DevTools MCP tools available -->
navigate_page, click, fill, fill_form, take_screenshot, evaluate_script
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E verification journey — owner invite + user join + task assignment + task work + AI handoff + real-time updates</name>
  <files>.planning/phases/07-end-to-end-verification/07-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/07-end-to-end-verification/07-RESEARCH.md (full verification flow steps 1-24, known pitfalls, code examples)
    - .planning/phases/07-end-to-end-verification/07-CONTEXT.md (locked decisions on verification method, test accounts, evidence)
    - .planning/phases/07-end-to-end-verification/07-01-SUMMARY.md (confirm members 403 fix is deployed)
  </read_first>
  <action>
Execute the following E2E journey using Chrome DevTools MCP against the live Vercel deployment. Take screenshots at each numbered checkpoint. Create the screenshots directory first: `.planning/phases/07-end-to-end-verification/screenshots/`.

**Pre-flight check:**
- `curl -sf https://paperclip-paperclip-api.qiwa34.easypanel.host/api/health` returns 200 with `"status":"ok"`
- If health check fails, wait 60 seconds and retry (Easypanel may still be rebuilding from Plan 01)

**Session A — Owner (existing account from Phase 6):**

1. Navigate to `https://paperclip-beige-five.vercel.app`
2. Sign in with the existing owner account (the one created during Phase 6 AUTH-05 verification)
3. Navigate to Company Settings page
4. Click "Generate Invite Link" (the human invite section)
5. SCREENSHOT: `01-invite-link-generated.png` — shows the invite URL is visible and copyable
6. Copy the invite URL from the page (it will be a relative path like `/invite/<token>` — prepend the Vercel origin to build full URL)
7. **E2E-01 CHECK:** Invite link was successfully generated and displayed

**If E2E-01 fails with 403 on the invite creation:**
The owner may lack `users:invite` permission in the deployed DB. This needs a manual SQL insert into Supabase's `principal_permission_grants` table. Flag this as a blocker and document in VERIFICATION.md.

**Session B — Invited User (new browser context / incognito):**

8. Navigate to the full invite URL: `https://paperclip-beige-five.vercel.app/invite/<token>`
9. The InviteLandingPage should appear. Click the sign-up / create account button
10. Fill in sign-up form with a real second email address and password
11. After sign-up, accept the invite (the page should show a "Join" button or auto-accept)
12. SCREENSHOT: `02-invite-accepted.png` — shows the invite was accepted and user is being redirected
13. Confirm the user is redirected to the home page (human joins get auto-approval via `resolveHumanJoinStatus`)
14. Navigate to My Tasks page: `/<company-prefix>/my-tasks`
15. SCREENSHOT: `03-my-tasks-empty.png` — My Tasks page loads (may be empty if no tasks assigned yet)
16. **E2E-02 CHECK:** User signed up, accepted invite, and sees the My Tasks dashboard

**Back to Session A — Owner (assign task):**

17. Create a new task/issue in the company (if none exist) using the New Issue dialog
18. Open the task detail and use the assignee picker to assign it to the invited user
19. SCREENSHOT: `04-task-assigned-to-user.png` — shows the task with the invited user as assignee
20. **E2E-03 CHECK:** Owner assigned a task to the invited user

**Back to Session B — Invited User (work task):**

21. Navigate to My Tasks — the assigned task should now appear
22. SCREENSHOT: `05-my-tasks-populated.png` — shows the assigned task in My Tasks
23. Open the task detail

24. **Status change:** Change the task status (e.g., from todo to in_progress) using the status selector
25. SCREENSHOT: `06-status-changed.png` — shows the new status

26. **File attach:** Upload a file using the attachment section (any small file, e.g., a text file)
27. SCREENSHOT: `07-file-attached.png` — shows the attachment in the issue detail

28. **Subtask creation:** Create a subtask using the subtask creation UI
29. SCREENSHOT: `08-subtask-created.png` — shows the subtask in the issue detail

30. Reload the page and confirm all three changes persist (status, file, subtask)
31. SCREENSHOT: `09-changes-persisted-after-reload.png` — shows all three still present after reload
32. **E2E-04 CHECK:** Status change, file attach, and subtask all persist after reload

33. **AI handoff:** Use the assignee picker to reassign the task to an AI agent
34. Confirm the API call succeeds and the task now shows the AI agent as assignee
35. SCREENSHOT: `10-reassigned-to-ai-agent.png` — shows the AI agent as the new assignee
36. **E2E-05 CHECK:** Task reassigned to AI agent; assignee picker updated

**WebSocket Verification (E2E-06):**

37. In Session A (owner): change any task's status (e.g., a different task, or the same one if re-assigned back)
38. Switch to Session B without refreshing: verify the status change is visible (TanStack Query cache invalidated via LiveUpdatesProvider)
39. SCREENSHOT: `11-realtime-update-visible.png` — shows both sessions with consistent state
40. **E2E-06 CHECK:** Real-time WebSocket update visible without page refresh

If any step fails, do NOT stop. Document the failure in VERIFICATION.md and continue with remaining steps. For small bugs (< 50 lines), fix inline, push, wait for rebuild, and retry. For larger bugs, document as a blocker.

**After all checks, produce VERIFICATION.md:**

Create `.planning/phases/07-end-to-end-verification/07-VERIFICATION.md` with:

```markdown
---
phase: 07-end-to-end-verification
verified_at: [ISO timestamp]
status: [all_pass | partial_pass | blocked]
---

# Phase 07 — End-to-End Verification Report

## Summary
[N/6 requirements passed]

## Results

### E2E-01: Owner can invite a new user
**Status:** [PASS/FAIL]
**Evidence:** screenshots/01-invite-link-generated.png
**Notes:** [any observations]

### E2E-02: Invited user can sign up, accept invite, see dashboard
**Status:** [PASS/FAIL]
**Evidence:** screenshots/02-invite-accepted.png, screenshots/03-my-tasks-empty.png
**Notes:** [any observations]

[... repeat for E2E-03 through E2E-06 ...]

## Bugs Fixed During Verification
[List any inline fixes made, with commit hashes]

## Blockers
[List any issues that could not be fixed inline]

## Sign-Off
- [ ] User has reviewed all screenshots
- [ ] User confirms all 6 E2E requirements pass
- [ ] Phase 7 is complete
```
  </action>
  <verify>
    <automated>ls /Users/pacosemino/Desktop/Paperclip/paperclip/.planning/phases/07-end-to-end-verification/07-VERIFICATION.md && ls /Users/pacosemino/Desktop/Paperclip/paperclip/.planning/phases/07-end-to-end-verification/screenshots/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-end-to-end-verification/07-VERIFICATION.md` exists
    - VERIFICATION.md contains "E2E-01" through "E2E-06" section headers
    - VERIFICATION.md contains a `status:` field in the frontmatter (one of: all_pass, partial_pass, blocked)
    - The `screenshots/` directory contains at least 6 .png files (one per E2E requirement minimum)
    - Each E2E requirement section has a Status field with PASS or FAIL
  </acceptance_criteria>
  <done>All 6 E2E requirements have been tested on the live deployment. VERIFICATION.md documents the pass/fail status for each with screenshot evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews VERIFICATION.md and gives final sign-off</name>
  <files>.planning/phases/07-end-to-end-verification/07-VERIFICATION.md</files>
  <action>
Present the VERIFICATION.md report to the user for review. The user must confirm all 6 E2E requirements pass before Phase 7 closes. If any requirement shows FAIL, the user decides whether to create a gap closure plan or accept the partial result.
  </action>
  <verify>
    <automated>grep -c "PASS\|FAIL" /Users/pacosemino/Desktop/Paperclip/paperclip/.planning/phases/07-end-to-end-verification/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - User has reviewed `.planning/phases/07-end-to-end-verification/07-VERIFICATION.md`
    - User has reviewed screenshots in `.planning/phases/07-end-to-end-verification/screenshots/`
    - User has typed "approved" or described issues to fix
  </acceptance_criteria>
  <what-built>
Complete E2E verification of the live deployment (Vercel + Easypanel + Supabase):
- E2E-01: Owner invite generation
- E2E-02: User sign-up + invite accept + My Tasks dashboard
- E2E-03: Task assignment to invited user
- E2E-04: Status change, file attach, subtask creation (with persistence)
- E2E-05: Task reassignment to AI agent
- E2E-06: Real-time WebSocket updates across sessions

Results documented in `.planning/phases/07-end-to-end-verification/07-VERIFICATION.md` with screenshots in `screenshots/`.
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/07-end-to-end-verification/07-VERIFICATION.md`
2. Review each screenshot referenced in the report
3. Confirm each E2E requirement has PASS status (or review FAIL explanations)
4. If any requirement shows FAIL: decide whether to fix inline or create a gap closure plan
5. If all PASS: approve to close Phase 7
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 7, or describe issues that need fixing</resume-signal>
  <done>User has reviewed the VERIFICATION.md report and given final sign-off for Phase 7 closure.</done>
</task>

</tasks>

<verification>
- `.planning/phases/07-end-to-end-verification/07-VERIFICATION.md` exists with status field
- All 6 E2E requirement sections present with PASS/FAIL status
- Screenshot directory contains evidence files
- User has reviewed and approved the verification report
</verification>

<success_criteria>
- All 6 E2E requirements verified on the live deployment: E2E-01 (invite), E2E-02 (join), E2E-03 (assign), E2E-04 (work), E2E-05 (AI handoff), E2E-06 (real-time)
- VERIFICATION.md produced with screenshot evidence
- User has given final sign-off
- Phase 7 is ready to close
</success_criteria>

<output>
After completion, create `.planning/phases/07-end-to-end-verification/07-02-SUMMARY.md`
</output>
