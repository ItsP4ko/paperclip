---
phase: 11-backend-deploy-gaps
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified: []
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03

must_haves:
  truths:
    - "Knowledge Base, Cost Recommendations, and Pipelines API routes return non-404 responses on live Easypanel backend"
    - "Sidebar navigation for Knowledge Base, Cost Optimizer, and Pipelines works end-to-end on the live deployment"
    - "Phase 3 critical path (owner sees team list with human members) passes on live deployment"
    - "Phase 4 critical path (invite -> join -> assign flow) passes on live deployment"
    - "Phase 5 critical path (cross-origin auth cookie persists) passes on live deployment"
  artifacts: []
  key_links:
    - from: "git push origin master"
      to: "Easypanel auto-rebuild"
      via: "Easypanel git deploy hook"
      pattern: "git push"
    - from: "Live backend /api/companies/:id/knowledge"
      to: "knowledgeRoutes handler"
      via: "Express route mount"
      pattern: "non-404 response"
    - from: "Live backend /api/companies/:id/pipelines"
      to: "pipelineRoutes handler"
      via: "Express route mount"
      pattern: "non-404 response"
---

<objective>
Push the atomic commit to master to trigger Easypanel rebuild, verify all three new API routes are live (non-404), and run E2E smoke tests for phases 3, 4, and 5 on the live deployment.

Purpose: Confirm the deploy actually worked — routes are registered, sidebar navigation resolves correctly, and previously-verified critical paths still pass.
Output: Live backend with all routes active, E2E smoke tests passing.
</objective>

<execution_context>
@/Users/pacosemino/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pacosemino/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2-phases/11-backend-deploy-gaps/11-CONTEXT.md
@.planning/milestones/v1.2-phases/11-backend-deploy-gaps/11-01-SUMMARY.md
@.planning/milestones/v1.1-phases/07-end-to-end-verification/07-VERIFICATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push to master and wait for Easypanel rebuild</name>
  <files></files>
  <read_first>
    - .planning/milestones/v1.2-phases/11-backend-deploy-gaps/11-01-SUMMARY.md
  </read_first>
  <action>
Run `git push origin master` to push the atomic commit from Plan 01. Easypanel is configured to auto-rebuild on new commits to master.

After pushing, wait approximately 60-90 seconds for the build to complete. Then verify the backend is responding by hitting the health endpoint:

```bash
curl -s -o /dev/null -w "%{http_code}" https://paperclip.pacosemino.com/api/health
```

Expected: HTTP 200.

If the health check fails after 90 seconds, retry up to 3 times with 30-second intervals (build may take longer).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://paperclip.pacosemino.com/api/health</automated>
  </verify>
  <acceptance_criteria>
    - `git push origin master` succeeds (no rejected refs)
    - Health endpoint returns HTTP 200 within 3 minutes of push
  </acceptance_criteria>
  <done>Code is pushed to master and Easypanel backend is responding to health checks.</done>
</task>

<task type="auto">
  <name>Task 2: Verify new API routes return non-404 on live backend</name>
  <files></files>
  <read_first>
    - .planning/milestones/v1.2-phases/11-backend-deploy-gaps/11-CONTEXT.md
  </read_first>
  <action>
Run curl commands against the live Easypanel backend for each of the three new route groups. Any response other than 404 (including 401, 403, or 200) counts as PASS because it means the route is registered and the handler is active.

Determine the company ID to use from prior E2E context or by querying the API. Then run:

```bash
# Knowledge Base route
curl -s -o /dev/null -w "%{http_code}" https://paperclip.pacosemino.com/api/companies/<COMPANY_ID>/knowledge

# Cost Recommendations route
curl -s -o /dev/null -w "%{http_code}" https://paperclip.pacosemino.com/api/companies/<COMPANY_ID>/cost-recommendations

# Pipelines route
curl -s -o /dev/null -w "%{http_code}" https://paperclip.pacosemino.com/api/companies/<COMPANY_ID>/pipelines
```

Replace `<COMPANY_ID>` with the actual company UUID. If the exact company ID is unknown, query `GET /api/companies` first (may need auth header).

Pass criteria: ALL three return a status code that is NOT 404. Expected codes: 200, 401, or 403.

If any route returns 404, check Easypanel build logs for compilation errors and report to user.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://paperclip.pacosemino.com/api/companies/test/knowledge | grep -v 404 && curl -s -o /dev/null -w "%{http_code}" https://paperclip.pacosemino.com/api/companies/test/cost-recommendations | grep -v 404 && curl -s -o /dev/null -w "%{http_code}" https://paperclip.pacosemino.com/api/companies/test/pipelines | grep -v 404</automated>
  </verify>
  <acceptance_criteria>
    - `curl ... /api/companies/{id}/knowledge` returns HTTP status != 404
    - `curl ... /api/companies/{id}/cost-recommendations` returns HTTP status != 404
    - `curl ... /api/companies/{id}/pipelines` returns HTTP status != 404
    - All three routes are confirmed registered on the live backend
  </acceptance_criteria>
  <done>All three new API route groups (knowledge, cost-recommendations, pipelines) return non-404 responses on the live Easypanel backend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: E2E smoke test for phases 3, 4, and 5 on live deployment</name>
  <files></files>
  <read_first>
    - .planning/milestones/v1.1-phases/07-end-to-end-verification/07-VERIFICATION.md
  </read_first>
  <action>
Present the E2E smoke test checklist to the user for manual verification on the live deployment. The checklist covers Phase 3 (team visibility), Phase 4 (invite flow), Phase 5 (cross-origin auth), and sidebar routing for the three new routes. All checks are manual browser-based tests against the live Vercel frontend and Easypanel backend.
  </action>
  <what-built>Backend redeployed with all routes active, sidebar routing fixed with UnprefixedBoardRedirect entries, pipelines migration executed</what-built>
  <how-to-verify>
**Open the live Vercel frontend in a browser and run these manual checks:**

### Phase 3 -- Owner Team Visibility
1. Log in as the owner account
2. Navigate to Org page (sidebar -> Company -> Org)
3. Verify the team list shows human members (not just AI agents)
4. Expected: At least 1 human team member visible (e.g., "Test User E2E" from Phase 7 verification)

### Phase 4 -- Multi-User Auth (invite -> join -> assign)
1. Still logged in as owner
2. Navigate to Company Settings -> generate a new human invite link
3. Open the invite link in an incognito/private window
4. Create a new account (or use existing test account) and accept the invite
5. Verify the invited user can see the company dashboard
6. As owner: assign a task to the invited user
7. As invited user: verify the task appears in My Tasks or Issues list

### Phase 5 -- Cross-Origin Auth (cookie persistence)
1. On the Vercel frontend, log in
2. Navigate between multiple pages (Dashboard -> Issues -> Agents -> Dashboard)
3. Verify you are NOT redirected to the auth page during navigation
4. Verify no 401 errors in the browser console during navigation
5. Expected: Auth cookie persists across all page navigations

### Sidebar Routing (DEPLOY-02)
1. Click "Knowledge Base" in the sidebar
2. Verify URL changes to `/{PREFIX}/knowledge` (not bare `/knowledge`)
3. Click "Cost Optimizer" in the sidebar
4. Verify URL changes to `/{PREFIX}/cost-recommendations`
5. Click "Pipelines" in the sidebar
6. Verify URL changes to `/{PREFIX}/pipelines`
7. None of these should show a 404 or blank page
  </how-to-verify>
  <resume-signal>Report results for each check: "Phase 3: PASS/FAIL, Phase 4: PASS/FAIL, Phase 5: PASS/FAIL, Sidebar: PASS/FAIL" with any failure details</resume-signal>
  <verify>
    <automated>echo "Manual checkpoint — user verifies E2E smoke tests on live deployment"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 3 check: Owner sees human team members in Org page
    - Phase 4 check: Invite -> join -> task assign flow completes (or user confirms prior verification still valid)
    - Phase 5 check: Auth cookie persists across page navigations on Vercel frontend
    - Sidebar routing: Knowledge Base, Cost Optimizer, and Pipelines links navigate to company-prefixed URLs
  </acceptance_criteria>
  <done>All E2E smoke tests pass on the live deployment. Phase 11 success criteria are met.</done>
</task>

</tasks>

<verification>
- Health endpoint returns 200 after deploy
- All 3 new API routes return non-404
- Phase 3, 4, 5 E2E smoke tests pass (user confirmed)
- Sidebar links for new routes work correctly (user confirmed)
</verification>

<success_criteria>
1. Easypanel backend is redeployed and healthy
2. Knowledge Base, Cost Recommendations, and Pipelines routes are live (non-404)
3. Phase 3, 4, 5 E2E smoke tests pass on the live deployment
4. Sidebar navigation works end-to-end for all new routes
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2-phases/11-backend-deploy-gaps/11-02-SUMMARY.md`
</output>
